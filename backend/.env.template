POSTGRES_DB=<postgres db name>
POSTGRES_USER=<postgres db user>
POSTGRES_PASSWORD=<postgres db password>
POSTGRES_PORT=<postgres port>
POSTGRES_HOST=localhost

REDIS_PORT=<redis port>
REDIS_ADDR=localhost:6379

CLIENT_ID=<strava api client id>
CLIENT_SECRET=<strava api client secret>
SUBSCRIPTION_ID=<strava subscription id>
VERIFY_TOKEN=<strava webhooks verify_token>

JWT_SECRET=<jwt secret>

FRONTEND_WEB_URL=<frontend web url>
FRONTEND_MOBILE_URL=<frontend mobile url>